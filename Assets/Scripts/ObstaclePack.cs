using System;
using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;
using UnityEngine.Serialization;
using Debug = System.Diagnostics.Debug;

public class ObstaclePack : MonoBehaviour
{
   public int Int;
   // public Human checkHuman;
   public int index;
   
   // private List<int> NumInt = new List<int>();
   // private void OnTriggerEnter(Collider other)
   // {
   //    if (other.gameObject.TryGetComponent(out Human human))
   //    {
   //       PlayerTower playerTower = human.gameObject.GetComponentInParent<PlayerTower>();
   //       List<Human> listHumans = playerTower._humans;
   //       // checkHuman = human;
   //       
   //       // if (listHumans != null)
   //       // {
   //       //    // index = PlayerTower.instance._humans.FindIndex();
   //       // }
   //    }
   
}
